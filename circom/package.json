{
    "name": "circuits",
    "type": "module",
    "private": true,
    "scripts": {
        "start": "node scripts/generate-proof.js",
        "run:ceremony": "bash scripts/run-ceremony.sh semaphore-v4",
        "compile:v4": "circom semaphore-v4/index.circom --r1cs",
        "compile:v3": "circom semaphore-v3/index.circom --r1cs",
        "compile:preimage-proof": "circom preimage-proof/index.circom --r1cs"
    },
    "dependencies": {
        "circomlib": "^2.0.2"
    },
    "devDependencies": {
        "@semaphore-research/merkle-tree": "0.2.0",
        "poseidon-lite": "^0.2.0",
        "snarkjs": "^0.7.0"
    }
}
